name: midterm_practice
description: A Flutter Notes practice app.
publish_to: "none"

version: 0.0.1

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  args: ^2.5.0
  provider: ^6.1.2
  flutter_bloc: ^8.1.4
  go_router: ^14.1.3
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
